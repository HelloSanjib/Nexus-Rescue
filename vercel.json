{
  "buildCommand": "cd Nexus-Rescue && npm install && npm run build",
  "outputDirectory": "Nexus-Rescue/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
